# Changelog — Grupo Higienissa · Wireframes

Formato: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) + [Semantic Versioning](https://semver.org/)

---

## [Unreleased]

- style: **Sectores + Recursos (hubs)** — el hero era un bloque centrado de 780px flotando en ~500px de vacío blanco (se leía como error de render). Ahora **header editorial a 2 columnas** (título display izq / lead + CTA der) que llena el ancho, vía clase reutilizable `.hub-head` sobre `.page-hero`. Verificado en vivo
- style: **Pacífica** — "Nuestra diferencia" (4 `pillar`-cards con icono) → **columnas editoriales** `.value-cols` (consistente con Home/Institucional). **Operissa** revisada: su estructura ya está variada (bento + sector-cards + pasos numerados), no tiene grid de cards repetido → sin cambios. Verificado en vivo
- refactor: **Trazatex — recorte fuerte** — eliminadas 3 secciones: "Quiénes somos", "Sectores que atendemos" (repetía los mismos sector-cards de Home/Institucional/cada sector) y "Ecosistema/Trazatex no trabaja solo" (cross-sell que se repite en otras marcas); "Qué información genera" **fusionada** dentro de "El retorno" (una sección en vez de dos). Altura **10.7k→8.0k px (~26%)**; sumado a retirar `/soluciones`, la historia RFID pasó de ~17.6k px en 2 páginas a ~8k en 1. Mantiene lo que el cliente pidió sí o sí (Infraestructura tecnológica, base instalada `#respaldo`). Verificado ES 200
- refactor: **consolidación RFID** — se retira `/soluciones` (duplicaba la historia RFID de Trazatex y estaba huérfana, sin entrada en el nav) con **redirect 301** ES/EN → `/marca-trazatex` (canónica); se borra `pages/soluciones.vue`. En Trazatex: quitado el `moreLink` → /soluciones (ya redundante), "Qué información genera" (8 `data-cards`) → lista `.def-cols`, quitada la fila de 4 highlights de "Quiénes somos" (duplicaban el bento de Soluciones de abajo), y 2 side-stripes más (`.problem-conclusion`, `.respaldo-note`). **Nota**: la altura bajó poco (10.7k→10.4k px) porque el largo es por **contenido** (13 secciones, varias pedidas por el cliente), no chrome — un recorte "fuerte" real requiere eliminar secciones enteras (pendiente decisión). Verificado: 301 OK, ES/EN 200
- style: **Institucional — dieta de cards** (era el peor caso: 4 grids de cards con icono apilados). "Capacidades" (7 `feature`-cards, dejaba una huérfana en la 3ª fila) → **lista de definición densa a 2 columnas** (`.def-cols`, sin cajas ni huérfana); "Qué nos distingue" (4 `pillar`-cards) → **columnas editoriales** (`.value-cols`, mismo patrón que Home). Promovido el patrón editorial de Home a **clase reutilizable** `.value-cols` (+ `.def-cols`) en `sections.css`; `PillarsGrid` refactorizado a usarla. Los 4 pasos numerados quedan como están (patrón distinto = variedad). Verificado en vivo
- style: **Home — "Lo que nos define"** (`PillarsGrid`) de cards con borde a **columnas editoriales con regla superior** (sin caja). Rompe el eco visual con los 3 highlights de `GroupIntro` (eran dos filas casi idénticas de icono+título+texto) y le da peso de manifiesto. Iconos y copy intactos. Verificado en vivo
- style: **fundaciones del barrido UI "afinar el carril"** (F0, global) — cargar la fuente **Inter** de verdad (estaba declarada en body/nav/botones/kickers pero nunca embebida; el texto caía a system-ui y variaba por SO) vía `<link>` a Google Fonts (400/500/600/700, `display=swap`, con `preconnect`); ampliar la escala tipográfica (`--fs-h3` 1.15→1.3rem; los subtítulos quedaban a 1.15× del cuerpo, por debajo del mínimo 1.25); **eliminar los side-stripes restantes** (`.statement`, `.prose blockquote`, `.problem-punch`, `.disclaimer` → panel/borde completo; misma regla "sin barras de color" ya aplicada a las cards y a `FromTo`); fix `--header-h` 64→72 (alto real del header; corrige offsets de `split-sticky`). Verificado en vivo (Inter renderiza, layout intacto)
- feat: **rediseño UI/UX de todo el sitio a un sistema visual unificado** — se elevan todas las páginas (marcas Pacífica/Operissa/Trazatex, Institucional, Soluciones, hub Sectores, sectores Hotelería/Industria/Minería, Recursos, Artículo) al estándar de Home/Salud. Nuevo componente **`SolutionsShowcase`** (bento con destacada ≤5 / grid uniforme 6+, sin cards huérfanas, números celeste serif) y composable **`useSplitTwo`** (títulos de card a 2 líneas balanceadas). Patrones aplicados de forma consistente: hero rico con badge flotante, split foto+texto, **intro editorial 2 columnas** para secciones de texto denso (título izq / texto der), **callout de cierre**, `ImageBand` centrada con foto de fondo, sector-cards y brand-cards fotográficas, feature-cards con tile de icono + **checklists ✓ alineados**, paneles con contraste (Menos/Más), **split-sticky** (problema: encuadre + evidencia), divisor de región + cards en la base instalada ASIS. Convención de headers (grids centrados · splits/intros densos a la izquierda) vía fix global de `.section-head` + `text-wrap:balance` global en h2. **24 fotos nuevas** curadas de banco libre (Pexels) por marca/sector/blog, optimizadas ≤250KB, con créditos en `CREDITS.md` y `content/pages/creditos.json`. Sin alterar copy (salvo hoteles "★" → "estrellas"). Verificado: 15 rutas 200 en ES/EN, 0 errores de consola
- feat: **sistema de movimiento de fondo `.bg-motion`** (deriva/olas de glows navy, reutilizable y afinable por `--motion-dur`/`--m1-3`, respeta `prefers-reduced-motion`) — aplicado al **banner "Solicitar evaluación"** (`FinalCta`, base navy de marca en lugar del azul plano) y a la banda **"De estimar a controlar"** de Salud. El CTA queda como único "momento" en movimiento
- feat: **fotos de servicios reemplazadas** — Pacífica (planta de lavandería industrial moderna), Operissa (personal de blanco operando + carros) y Trazatex (operario con lector de mano + tablet; reemplaza la foto de uniforme militar). Optimizadas (~160–235KB, 1600px); créditos actualizados en `content/pages/creditos.json` y `CREDITS.md`. *(Trazatex es una mejora interina: el stock libre no tiene RFID en contexto clínico-textil; pendiente foto propia o key Pexels/Unsplash)*
- style: **FromTo** ("Qué cambia") — quitada la barra de color eléctrico de la card "ahora" (**regla de diseño: sin bordes/barras de color en cards**); card "antes" pasada de fondo negro a claro (`--bg-alt`/`--muted`) e igualada en alto a la card "ahora" (`align-items: stretch`)
- refactor: **home — eliminada la franja "En alianza con ASIS IDTRAK"** (`AsisStrip`); evita dos bandas navy consecutivas y deja el CTA final como cierre
- style: **Sector Salud — auditoría Impeccable (Rowena) aplicada** — rebalanceo de la sección **Riesgos** (la columna sticky dejaba ~450px de vacío muerto): frase-punch "tres dimensiones de riesgo" bajo la cabecera (reutiliza `.problem-punch`) + grid cerrado a `1fr 1.4fr`; **marcadores de riesgo** a `var(--azul)` (contraste AA; antes khaki `--gris-marca` ~2.3:1); `FromTo` **sin el tachado punteado** (se leía como glitch cruzando el texto), texto "antes" a alpha `.72` (AA) + caja con más cuerpo (padding + min-height) para equilibrar con "ahora"; `.btn-ghost` con **borde perceptible** en reposo (`color-mix` azul/línea, antes `--line` invisible). Fotos (hero/Trazatex/Pacífica/impacto) pendientes de recuración de assets
- feat: **Sector Salud — 2ª iteración (plan de Rowena + feedback del cliente)** — ritmo de imágenes alternado (`.intro-split.reverse`, sin eco hero→contexto), riesgos agrupados por dimensión (Clínico/Cumplimiento/Costo), banda-declaración sin foto (gradiente), comparativa "de→a" (`FromTo`) con antes recesivo/ahora dominante, panel de medición (`MeasureBoard`) para "lo que pasa a ser medible", lista editorial (`StakeList`), marcas con jerarquía (eyebrow+rol). 4 componentes/patrones nuevos reutilizables + plugin `reveal` robustecido (revela lo ya visible sin depender de IO)
- feat: **Sector Salud elevado a nivel Home (piloto de mejora)** — hero rico con foto clínica real + badge flotante, sección de contexto foto+texto (pasillo hospitalario), nueva **banda a sangre `ImageBand`** con overlay como momento visual, indicadores convertidos de chips planos a feature-grid con iconos, impacto por rol con iconos. 3 fotos clínicas nuevas (banco gratuito, verificadas/optimizadas, atribución en `/creditos`). Componente `ImageBand` reutilizable para el resto de plantillas
- fix: **puesta en marcha** — Nuxt fijado a `~3.13.2` (3.14 traía un `unhead` sin `getActiveHead` que rompía la hidratación con `@nuxtjs/i18n` v8); contenido cargado como `?raw`+`JSON.parse` (evita el bug `orgTransform.apply` del transform de JSON de Vite); páginas admin sin slot `#title` inválido; `BaseIcon` dimensiona por CSS (SVG width/height read-only); plugin `reveal` diferido post-hidratación (sin class mismatch); auth degrada con gracia sin Firebase configurado. Verificado: 17 rutas 200, 0 errores de consola en públicas
- feat: **16 plantillas públicas bilingües (ES/EN)** — home, 3 marcas (Trazatex con problema/4 capas/infra/retorno/#respaldo+disclaimer), hub + 4 sectores, soluciones, institucional, recursos+artículo, contacto, legales y `/creditos`. Cada una = JSON de contenido `{es,en}` (verbatim del wireframe) + página Vue reutilizando la librería. Paridad EN completa verificada
- feat: **contacto administrable end-to-end** — `ContactForm` (vee-validate + Zod, `.max` en todos los campos) → server route `/api/contact` (valida con el mismo schema y escribe en `contactSubmissions` vía firebase-admin, sin SDK en el bundle público)
- feat: **admin SPA `/admin`** — auth Firebase (rol en `users/{uid}`), editor de páginas por bloque (reordenables con vuedraggable, editor bilingüe recursivo, draft/publish), bandeja de contactos; UI propia (Modal/Toast/Confirm, nunca dialogs nativos); `firestore.rules`/`storage.rules`/índices y Cloud Functions `publishPage` (+ rebuild Vercel) + denormalización
- feat: SEO/i18n — hreflang + canonical automáticos (`useLocaleHead`), `useSeoMeta` por plantilla, `robots.txt`, `/creditos` con atribución CC + link en footer
- feat: **rediseño visual M3 "corporativo limpio" + fotografía real** — nuevo sistema `assets/styles/site.css` (blanco/gris claro, azul de acento, líneas finas, sombras suaves, cards con foto, sectores con overlay, reveal on-scroll, contraste AA). 10 fotos de banco gratuito verificadas y optimizadas (24MB→3MB) en `assets/img/`; `CREDITS.md` con atribuciones CC. Aplicado a la Home como referencia aprobada
- feat: **scaffold del proyecto de producción en Nuxt 3 (JS nativo)** — `nuxt.config.js` con render híbrido (`routeRules`: público prerender / `/admin` SPA), `@nuxtjs/i18n` `prefix` ES/EN, `runtimeConfig` Firebase, `.env`/`.env.example`; wireframes estáticos movidos a `_reference/`; `DB.md` (esquema Firestore fuente de verdad); composables de contenido bilingüe (`useT`, `useSiteContent`); librería de componentes (átomos + chrome accesible + 7 secciones del Home); Home portada a Vue consumiendo `content/pages/home.json`
- docs: añadidos `HOME.md` (detalle del Home) e `IMPLEMENTATION_PLAN.md` (plan del front Nuxt+Firebase, decisiones, fases) como referencia de la migración a M3/producción; `.vercelignore` los excluye del deploy. El wireframe queda como **fuente de verdad de contenido/estructura** del nuevo proyecto `higienissa-front`
- feat: **página Trazatex — versión simple del bloque "Infraestructura tecnológica"** (definitiva, pedida por el cliente "sí o sí") — vuelve a las **3 tarjetas** Hardware/Software/Servicios con la intro de 2 párrafos y el párrafo de cierre; kicker "Infraestructura tecnológica". Reemplaza el ensayo de 3 sub-bloques detallados (Infraestructura/Plataforma/Servicios). Se **restauró "Qué información genera"** (8 datos + 3 dashboards de ejemplo) justo después, que con la versión simple ya **no solapa**. Quitado el CSS `.subhead`/`.card h4` (sin uso). Paridad ES↔EN; `04-content.md`
- fix: **`soluciones.html` — swap de capas 1↔2** en "Cuatro capas, un mismo dato", para que coincida con el orden de la página Trazatex: *Infraestructura de lectura* → Capa 01, *Identificación individual* → Capa 02. Paridad ES↔EN. *(El lead del hero y la lista "Incluye" de esa página siguen mencionando la identificación primero — pendiente de decidir si se alinean.)*
- refactor: **página Trazatex — pasada de estructura mid-fi** — para dar ritmo y jerarquía a una página que se sentía plana ("todo apilado, sin estructura"): (1) **activada la alternancia de fondos** (`.section-alt` con `--bg-alt` — la clase existía en el HTML pero no tenía CSS; hoy scopeada a `.page-trazatex` en `<body>` hasta validar, luego se hace global); (2) los **sub-bloques** de "La solución Trazatex" ganan divisor superior + aire (`.subhead` con `border-top`) para leerse como grupos distintos; (3) **dedup**: eliminada la sección independiente **"Qué información genera"** (repetía los datos de la plataforma) — sus 3 dashboards de ejemplo se movieron al sub-bloque "Plataforma de inteligencia operativa". Paridad ES↔EN; `04-content.md`
- feat: **página Trazatex — bloque "La solución Trazatex" con el detalle completo del cliente** — las 3 cards simples (Hardware/Software/Servicios) se reemplazan por los **3 sub-bloques detallados** que pidió el cliente, dentro de la misma cabecera "Tecnología, datos y experiencia…": *Infraestructura tecnológica* (Lectura masiva automatizada · Puntos de control fijos · Operación móvil, con descripción), *Plataforma de inteligencia operativa* (intro + 7 datos que permite visualizar + cierre) y *Servicios especializados* (5 cards con descripción: consultoría y diagnóstico, implementación y puesta en marcha, capacitación, soporte, optimización). Kicker cambiado a "La solución Trazatex" (evita duplicar el sub-bloque "Infraestructura tecnológica"). Reintroduce `.subhead` y `.card h4`. Paridad ES↔EN; `04-content.md`. **Pendiente:** los bullets de la plataforma solapan con "Qué información genera" (decidir si esa sección se recorta)
- fix: **página Trazatex** — el badge "Powered by ASIS IDTRAK" del hero ahora **enlaza a la sección de respaldo** (`#respaldo`, base instalada ASIS IDTRAK); hover de pill sin subrayado (`a.badge:hover`). Además, en "El modelo de 4 capas" se **intercambió el orden** de las dos primeras capas: *Infraestructura de lectura* pasa a ser la capa 1 e *Identificación individual* la 2. Paridad ES↔EN; `04-content.md` actualizado
- refactor: **página Trazatex — "El retorno"** — fusionadas las secciones "Lo que permite lograr" e "Impacto económico" en una sola (kicker "Resultados", h2 "Menos pérdidas, más control"), que decían el mismo mensaje en dos bloques casi gemelos. Las listas −/+ se unifican (cada beneficio aparece una sola vez; el encuadre "cuenta de resultados / gasto medible" pasa al lead) y la sección queda tras "Qué información genera". Narrativa: cómo funciona → con qué → qué datos da → qué se gana → dónde aplica. Paridad ES↔EN; `04-content.md` actualizado
- feat: **página Trazatex — sección "Infraestructura tecnológica" reescrita** con el copy final del cliente (13/06/26), tras "El modelo de 4 capas" y antes de "Qué información genera". Subtítulo *"Tecnología, datos y experiencia que hacen posible la trazabilidad"*, intro de 2 párrafos, las **3 cards** Hardware/Software/Servicios (con sus listas) y un **párrafo de cierre** bajo las cards ("Trazatex diseña, implementa y pone en funcionamiento…"). Nuevo estilo `.section-foot` (+ párrafo de apoyo bajo el lead) en `wireframe.css`; paridad ES↔EN; copy en `04-content.md`. *(Sustituye un ensayo intermedio de tres sub-bloques —Infraestructura/Plataforma de inteligencia operativa/Servicios con descripciones— que el cliente descartó en su 2ª versión; el detalle queda en el historial git.)*
- feat: **banda "El problema" de Trazatex ampliada** — el gancho ("¿Sabías que la mayoría… no sabe dónde están sus prendas?") quedaba como una idea suelta al cerrar con un solo párrafo; ahora arma el caso: encuadre ("sin trazabilidad el textil es un activo invisible; lo que no se mide no se controla") + lista de **6 síntomas concretos** (`marker-list risk`, 2 columnas) + diagnóstico de cierre con barra azul (`.problem-punch`, espejo del gancho celeste). Nuevos estilos `.problem-symptoms`/`.problem-punch`. Paridad ES↔EN
- feat: **página Trazatex enriquecida con el feedback del cliente (Gabriel, 10/06/26)** — su socio pedía no quedarse cortos en info relevante. Se añadió: banda **"El problema"** tras el hero (gancho de apertura "El problema no es la falta de ropa, es la falta de información"); **"Infraestructura tecnológica"** con 3 frentes (Hardware · Software · Servicios especializados) bajo el modelo de 4 capas; **"Lo que permite lograr"** (Menos pérdidas/compras/tiempo/contacto · Más control/información/productividad, con marcadores −/+); **"Qué información genera"** (8 datos concretos en cards + 3 slots de imagen de ejemplo) que reemplaza la antigua lista de chips "Indicadores"; e **"Impacto económico"** (reducción de pérdidas/compras/reclamos, ahorro de horas-hombre, evidencia para auditorías). Paridad ES↔EN completa
- chore: nuevos componentes en `wireframe.css` (`.statement`/`statement-q`/`statement-sub`/`statement-punch` para la banda del gancho, marcadores `.marker-list.less`/`.more`, `.example-grid`); copy registrado en `04-content.md`; sección Sectores de Trazatex pasada a fondo blanco para mantener el ritmo de alternancia
- refactor: página **Nosotros** — quitada la sección "Las tres divisiones" (repetía las marcas que ya están en la tesis del ecosistema; los enlaces a marca se movieron a los nodos del diagrama); "Capacidades" y "Sectores" expandidas de chips a **cards con descripción** (+ enlace "Ver sector" en sectores)
- refactor: `sectores.html` — quitada la línea "Marcas relevantes" de las cards del hub (todas las marcas aplican a todos los sectores, era redundante)
- feat: "Sectores que atendemos" en páginas de servicio (pacífica, operissa, trazatex, soluciones) pasa de chips a **cards con descripción + enlace "Ver sector"**; en soluciones se separó como sección propia (antes vivía dentro de "A quién aplica")
- feat: íconos SVG inline en los 3 pilares de "Lo que nos define" del home (componente `.pillar-icon`)
- style: quitada la franja azul (`border-left`) de los cards `.highlight` bajo el hero
- fix: home "Qué es el grupo" — el panel de destacados ya no nombra las marcas (se mencionan abajo); reemplazado por destacados de valor
- fix: filtro de categorías del blog (`recursos.html`) sin `margin-bottom` ni realce del chip activo — cambiado de `.chips` a `.filters` (componente con margen + estilo de chip activo)

- feat: heros más altos en todas las páginas (`.hero` a `clamp(64px, 8vw, 112px)` + `hero-grid min-height: 420px` + placeholder `tall` a 460px)
- feat: franja gris bajo el hero rediseñada con el patrón `intro-split` (texto a la izquierda + panel de destacados a la derecha) en home, las 3 marcas, soluciones y el "Contexto" de los 4 sectores
- feat: página **Nosotros** ampliada de 3 a 10 secciones — propósito, tesis del ecosistema (diagrama Procesa·Traza·Opera), qué integramos, sectores transversales y franja de respaldo ASIS IDTRAK (nombre enlazado a Trazatex)
- feat: `soluciones.html` enriquecida — nueva sección "El problema" (los textiles son activos) y "Cuatro capas" reconstruida como cards `.feature` (Capa 01–04 con detalle real: infraestructura de lectura, plataforma/KPIs)
- feat: secciones de sector con estructura real (`.marker-list` con variantes `ask`/`risk`/`gain` + indicadores como `chips`) en los 4 sectores — antes eran listas planas con clases inexistentes
- fix: doble numeración en "Cuatro capas" de soluciones — el `<ol class="steps">` mostraba el marcador nativo además del badge `.step-num`; resuelto con `list-style: none`
- fix: encabezado duplicado "Pensamos como operadores" en `marca-pacifica.html` — el `<h2>` de sección repetía el primer pilar; ahora es "Más que una lavandería tradicional"
- chore: nuevos componentes en `wireframe.css` (`intro-split`, `highlight-stack`/`highlight`, `marker-list`, `feature`) y paridad EN completa de todos los cambios
